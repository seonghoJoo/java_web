<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="crewMembers">
	<select id="selectOwnList" parameterType="int"  resultType="CrewMember">
		SELECT no, crew_no crewNo,user_no userNo,role,report_num reportNum,profile_img profileImg, introduction,type,regdate
		FROM crew_members
		WHERE user_no = #{no}
		ORDER BY 1
	</select>
	
	
	
</mapper>